# Scripts

## manual

Ad-hoc diagnostics and network/API sanity checks that are useful during development but not part of automated CI.

Examples:

- python scripts/manual/test_nhl.py
- python scripts/manual/test_shl_full.py

## maintenance

One-off maintenance/migration helpers used while evolving the codebase.

These are intentionally separate from runtime application code.
